# Training config for T4 GPUs — FAST version
# ByT5-small (300M params) with reduced data for practical Colab training
# Estimated: ~6-10 hours on T4

# Model — small is ~2x faster than base per step
model: "google/byt5-small"

# Data
train_data: "data/clue_answer_pairs/train.jsonl"
val_data: "data/clue_answer_pairs/val.jsonl"
max_answer_len: 25
max_input_len: 256
max_target_len: 32
online_augment: true # adds variety even with fewer static augmentations

# Training — byt5-small in fp32 needs smaller batch to fit 16GB VRAM
output_dir: "checkpoints/clue_answerer"
epochs: 3
batch_size: 4 # fp32 uses 2x memory vs fp16
gradient_accumulation_steps: 16 # effective batch = 64 (same as before)
lr: 5.0e-4
weight_decay: 0.01
warmup_steps: 200

# Precision — ByT5 overflows in fp16 on T4, must use fp32
fp16: false
bf16: false

# Saving / eval
save_steps: 2000
eval_steps: 2000
logging_steps: 100

# W&B
wandb_project: "conditional-bp-solver"
